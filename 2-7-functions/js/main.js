const greet = (myName, age, mentalAge) => {
  console.log(
    `Hello, my name is ${myName}!! I am ${age} years old, but my mental age is it ${mentalAge}`
  );
};

const myName = "Stephan";
let age = 32;
const mentalAge = "55";

greet(myName, age, mentalAge);
